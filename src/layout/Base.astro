---
import Footer from "../components/Footer.astro";
import Nav from "../components/Nav.astro";
import { ViewTransitions, slide } from "astro:transitions";

interface Props {
  title: string;
}
const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>QuejaCiudadana | {title}</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
      integrity="sha512-...TuCódigoDeIntegridad..."
      crossorigin="anonymous"
    />
    <ViewTransitions/>
  </head>
  <body class="bg-teal-50 dark:bg-zinc-800" transition:animate={slide({ duration: "1s" })}>
    <Nav />
    <main class="mx-auto container pt-10">
      <slot />
    </main>
    <Footer />
  </body>
</html>
