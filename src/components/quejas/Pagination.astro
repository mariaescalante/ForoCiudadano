---
const { currentPage, totalPages, disablePrevious, disableNext } = Astro.props;

interface Props {
  currentPage: number;
  totalPages: number;
  disablePrevious: boolean;
  disableNext: boolean;
}
---
<div class="flex justify-between mt-10 lg:mx-20 mx-5">
  <a href={'/quejas?page=' + (currentPage - 1)} class={
    disablePrevious ? "inline-block bg-gray-100 px-3 py-2 text-gray-400 rounded-lg pointer-events-none" : "inline-block bg-teal-600 px-3 py-2 hover:bg-teal-700 hover:text-white rounded-lg"
  }>
    Previous
  </a>
  <a href={'/quejas?page=' + (currentPage + 1)} 
    class={
      disableNext ? "inline-block bg-gray-100 px-3 py-2 text-gray-400 rounded-lg pointer-events-none" : "inline-block bg-teal-600 px-3 py-2 hover:bg-teal-700 hover:text-white rounded-lg"
    }>
    Next
  </a>
</div>